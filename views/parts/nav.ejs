<nav class="navbar navbar-expand-lg navbar-dark bg-dark align-self-start w-100">

    <a class="navbar-brand" href="#">Me 4 Life</a>
    <button class="navbar-toggler btn-outline-dark" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav text-center" style="margin: 0px auto;">
            <% if (loggedIn) { %>
                <li class="nav-item <% if (title === 'Home') { %>
                    active
                <% } %>">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a href="/user/api/logout" class="nav-link">Logout</a>
                </li>
            <% } %>
            
            <!--check if the user logged out -->
            <% if (!loggedIn) { %>
                <li class="nav-item <% if (title === 'Login') { %>
                        active
                    <% } %>">
                    <a class="nav-link" href="/user/login/">Login</a>
                </li>
                <li class="nav-item <% if (title === 'Signup') { %>
                        active
                    <% } %>">
                    <a class="nav-link" href="/user/signup/">Signup</a>
                </li>
            <% } %>
    </div>
</nav>